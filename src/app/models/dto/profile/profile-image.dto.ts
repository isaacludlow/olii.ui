export interface ProfileImage {
    /** Id for placement and/or order on the profile page. */
    Id: number;
    /** Urls to images. */
    Url: string;
    /** Base64 string representing downloaded images */
    DownloadedImage: string;
}